spring:
  profiles:
    default: local

  application:
    name: seeyouagain-be
  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: always
  health:
    db:
      enabled: true

logging:
  level:
    org.hibernate.SQL: info
    tetoandeggens.seeyouagainbe: info